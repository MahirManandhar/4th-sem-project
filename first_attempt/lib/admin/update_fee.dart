import 'package:flutter/material.dart';

class UpdateFee extends StatelessWidget {
  const UpdateFee({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
